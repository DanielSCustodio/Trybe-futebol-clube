import StatusCode from '../../enum/StatusCode';

const statusAndMessage = [

  {
    status: StatusCode.UNAUTHORIZED,
    messageReceived: 'Incorrect email or password',
    message: 'Incorrect email or password',
  },

];

export default statusAndMessage;
